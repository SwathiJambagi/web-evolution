// const navbar = document.getElementById('navbar1');

// window.addEventListener('scroll', () => {
//     if (window.scrollY > 50) {
//         navbar.classList.add('transparent'); // Make navbar transparent
//     } else {
//         navbar.classList.remove('transparent'); // Restore original background
//     }
// });